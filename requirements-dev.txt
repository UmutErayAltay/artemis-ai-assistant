# Testleri ve statik denetimleri çalıştırmak için GEREKEN paketler.
#
# `requirements.txt`'ten AYRI tutulmasının sebebi ölçüldü: tam liste
# PyQt6, faster-whisper (dolaylı olarak torch), av, vosk içeriyor —
# gigabaytlarca indirme. Testlerin hiçbiri bunlara ihtiyaç duymuyor;
# ses ve UI katmanları sahte modüllerle sınanıyor (bkz.
# tests/conftest.py::install_fake_module). Böylece CI dakikalar değil
# saniyeler sürüyor.
#
# Çalıştırma bağımlılıkları için: pip install -r requirements.txt

pydantic>=2.7
PyYAML>=6.0
psutil>=5.9
numpy>=1.24         # voice/audio.py testlerinin PCM örnekleri
mcp>=1.20           # plugins/mcp_plugin.py GERÇEK bir alt süreçle test edilir
requests>=2.31      # voice/stt_cloud.py ve stt_azure.py testleri modül seviyesinde import eder

pytest>=8.0
ruff>=0.6
mypy>=1.11

types-PyYAML>=6.0    # config/settings.py yaml.safe_load
types-requests>=2.31 # voice/stt_cloud.py, voice/stt_azure.py

# NOT: scripts/bench_tool_selection.py'nin ölçüm mantığı `toolbench` adlı
# bağımsız bir pakete taşındı (~/toolbench, kardeş local proje). Burada bir
# `-e ../toolbench` path bağımlılığı OLARAK EKLENMİŞTİ ama GitHub Actions
# CI'yi kırdı (CI bu repoyu izole klonluyor, ../toolbench orada yok — run
# 35584430941). Şimdilik `scripts/_toolbench_vendor.py` (elle senkronize
# kopya) kullanılıyor, ekstra bağımlılık gerekmiyor. `toolbench` PyPI'ye
# çıkınca (M3) burada gerçek bir `toolbench>=x` satırı eklenip vendor
# dosyası kaldırılacak.
